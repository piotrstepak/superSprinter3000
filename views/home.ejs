<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Super Sprinter 3000</title>
    <script></script>
    <link rel="stylesheet", href="stylesheets/main.css">
</head>
<body>
    <div class="container">
        <h1>Super Sprinter 3000</h1>
        <!-- <form action="/story" method="GET">
            <button type="submit" id="add-new-user-story">Add new User Story</button><br><br>
        </form> -->
        <a href="/story">Add new User Story</a><br><br>
        <table>
            <thead>
                <tr>
                    <th>Id</th>
                    <% for (let i = 0; i < data[0].length; i++) { %>
                    <th><%= data[0][i] %></th>
                    <% } %>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 1; i < data.length - 1; i++) { %>
                    <tr id="<%= i %>">
                    <td><%= i %></td>
                    <!-- <td><a href="/story/<%= i %>"><%= data[i][0] %></a></td> -->
                    <!-- <form action="/story/<%= i %>" method="POST">
                        <td><input type="submit" value="<%= data[i][0] %>" name="id"></td> 
                    </form> -->
                    <td><a href="/story/<%= i %>" id="<%= i %>"><%= data[i][0] %></a></td>
                    <!-- link zamiast tego forma, tylko co wtedy z id? -->
                    <% for (let j = 1; j < data[0].length; j++) { %>
                    <td><%= data[i][j] %></td>
                    <% } %>
                </tr>
               <% } %>
            </tbody>
        </table>
    </div>    
</body>
</html>